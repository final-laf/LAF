<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.laf.product.model.mapper.OptionMapper">

	<!-- 상품번호로 해당 상품의 모든 옵션 조회 -->
	<select id="selectOptionList" resultType="Option">
		SELECT  option_no,
				product_no,
				size,
				color,
				stock,
				location,
				sell_count
		FROM   `option`
		WHERE 	product_no = ${productNo}
	</select>
	
	<!-- 색상 선택 시 해당 색상 사이즈 목록 조회 -->
	<select id="getOptionSelectedColor" resultType="Option">
		SELECT  option_no, color, size, stock
		FROM   `option`
		WHERE 	product_no = ${productNo}
				AND color = #{color}
	</select>
	
</mapper>

