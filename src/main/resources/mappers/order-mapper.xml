<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.laf.order.model.mapper.OrderMapper">
    
    <!-- 주문자정보 -->
    <select id="orderMember" resultType="Member">
        SELECT * FROM member WHERE member_no= #{memberNo};
	</select>

    <!-- 주문상품정보 -->
    <select id="orderProduct" resultType="Product">
		SELECT product_name, product_price, product_sale_price, i.img_path, o.`size`, o.color
		FROM product AS p
		JOIN product_img AS i ON p.product_no = i.product_no
		JOIN `option` AS o ON p.product_no = o.product_no
		WHERE p.product_no = #{productNo}
		AND o.option_no = #{optionNo};
	</select>
   
</mapper>

